/**
 * WorkDistributionChart - Histogram of Daily Work Hours with Zone Coloring
 * 
 * This is a re-export of the CustomDistributionChart which uses pure SVG
 * for perfect alignment of histogram bars and normal distribution curve.
 */

import CustomDistributionChart from './CustomDistributionChart';

export default CustomDistributionChart;
